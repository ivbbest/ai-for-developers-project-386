# Learnings — извлечённые уроки

Копилка уроков проекта (append-only): особенности кода, грабли, решения,
которые нельзя терять между сессиями. Формат: `YYYY-MM-DD | урок | источник`.

- Добавление — **только в конец файла** (историю не чистить и не переписывать).
- В конце сессии — обязательный ритуал (см. `.agents/tasks.md` → «Правила ведения»):
  записать всё, с чем столкнулись, чтобы новый чат не переоткрыл закрытый вопрос.

## Записи

- 2026-08-30 | Коммит-сообщения пишем нейтрально: без слов «ИИ»/«AI» и подобных паттернов | AGENTS.md «Границы действий»
- 2026-08-30 | Реестр задач (`tasks.md`) держим локально в `.agents/`, а не в git: в публичном репо только выверенное (AGENTS.md, README, workflows); публичные задачи курса — в GitHub Issues | solution 2026-08-30
- 2026-08-30 | В сообщениях агента не упоминаем чужие проекты/репозитории, даже как источник скиллов — единственный контекст `/mnt/e/hexlet/cal-com` | AGENTS.md «Остальное»
- 2026-09-01 | opencode GitHub-интеграцию удобно разбивать на отдельные workflow-файлы по событиям (интерактив/триаж/ревью/schedule), а не смешивать в один | opencode docs + реализация
- 2026-09-01 | Для schedule/workflow_dispatch нет юзер-контекста → нужны явные write-права (contents/pull-requests/issues); для issues/pull_request-промпт-событий достаточно read (action пишет через App-токен OIDC) | opencode docs
- 2026-09-01 | PyYAML интерпретирует ключ `on` как булев `True` (специфика YAML 1.1) — валидировать workflow через структуру `jobs`/`runs-on`/`steps`, а не через `d['on']` | грабли 2026-09-01
- 2026-09-01 | Модель `opencode/big-pickle` не поддерживает image-input: скриншоты PNG читаем через OCR (tesseract, `-l rus+eng`) или передаём субагентам; для UI-скриншотов OCR даёт достаточно текста для реконструкции сценариев | грабли 2026-09-01
- 2026-09-01 | Окружение — WSL2, node/npm приходят из Windows-установки (nodejs из `/mnt/c/...`, npm prefix — в профиле Windows-пользователя): `node` на Linux-PATH не виден (exit 127), работают `npm`/`npx`-обёртки. Перед dev-сборкой/тестами проверить доступность node, иначе установка/запуск зависнет/упадёт | грабли 2026-09-01
- 2026-09-01 | Ревью: контракт «отдаём только свободные слоты» против UI «Свободно/Занято» — фронт не может показать занятое, чего нет в ответе API. Правило: ответ слотов = вся сетка дня со `status` | находка К1
- 2026-09-01 | Ревью: «на одно время две брони нельзя» при разных длительностях (15/30 мин) реализуется только проверкой пересечения интервалов (`newStart < exEnd && newEnd > exStart`); `UNIQUE(start)` пропускает конфликт 09:00–09:30 vs 09:15 | находка К2
- 2026-09-01 | Ревью: utcDateTime + «09:00–18:00» + «сегодня» без фиксированной TZ = разъезд фронта/бэка (браузер конвертирует UTC). Решение: TZ сервиса константой, форматирование в ней, не в поясе браузера | находка К4
- 2026-09-01 | Ревью: два документа описывали один контракт по-разному (`?date=` vs `?from&to`) — источник правды один; сверять спеку с планом §9 и скринами UI обязательно | процесс
- 2026-09-01 | Эмиттер `@typespec/openapi3` по умолчанию пишет JSON — для `dist/openapi.yaml` в `tspconfig.yaml` нужен `output-file-type: yaml`; версии пакетов пинить | грабли TypeSpec (проверить на факте генерации)
- 2026-09-01 | Docker-требование курса = одно приложение на `PORT`: бэк обязан раздавать и фронт (static + SPA-fallback); если этого нет в плане бэка, всплывает на шаге 5 | находка A1
- 2026-09-01 | Render free tier: ФС эфемерна — SQLite-файл стирается при рестарте/деплое; выход — идемпотентный seed при старте (данные демо не критичны) | находка A2
- 2026-09-01 | `input/` исключён из git локальным `.git/info/exclude` — невидим в `git status`; новой сессии не «чинить» и не коммитить | факт окружения
- 2026-09-01 | PII-гигиена публичной памяти: ФИО/email проверяются grep'ом по `.agents/` перед коммитом (в истории коммитов email остаётся в metadata — это отдельное решение пользователя) | практика
- 2026-09-01 | Класс расхождений «документы по отдельности верны, но не сверены»: лечится тремя артефактами — матрица «экран↔поле API», формула на каждое неформальное правило, трассировка «требование↔эндпоинт↔тест» | retrospective §1
- 2026-09-01 | Временны́е тесты только через injectable `now()` (иначе e2e стареют с календарём); касание интервалов 09:15–09:30 vs 09:00–09:15 НЕ конфликт (строгие </>) — тест обязателен | retrospective §2
- 2026-09-01 | Задача = поведение контракта + тест + один коммит; шаги «размером с фичу» не позволяют бисектить регрессию | retrospective §4
- 2026-09-01 | Git-workflow (решение пользователя): ветка `<тип>/<тема>`, коммит-чекпоинт на изменение; push и открытие PR — пользователь; мержит в main только пользователь; история из «big docs» коммитов не даёт линейной археологии — правки ревью разбиты на цепочку логических коммитов | решение 2026-09-01
- 2026-09-01 | Порты 3001/5173/4010 в dev могут быть заняты: приложение читает PORT/env, на EADDRINUSE — внятная ошибка; перед прогоном проверять `ss -ltnp` | требование пользователя
- 2026-09-01 | Окружение запрещает агенту `git push`/`fetch`/`pull` (permission deny) — агент коммитит в ветку, push и открытие PR делает пользователь; не пытаться пушить | факт окружения
- 2026-09-01 | Grep по одной строке пропускает формулировки, перенесённые через `\n`
  (промахнулся дважды: «PR открывает\nагент» в tasks.md и «PR открывает исполнитель» в
  §11). Для смысловых фраз — многострочный `grep -Pzo` или перенормализовать текст в
  одну строку; перед сдачей ветки перечитать правила, а не только грепануть | грабли ревью PR #2
- 2026-09-01 | Факты об инструментах проверять по первичному источнику (исходники/тарболы npm), а не по верхневному help: `tsp compile --warn-as-error` существует (в `tsp --help` не виден — опция подкоманды); Prism не хранит состояние (README: Data Persistence не реализована), но коды 4xx отдаёт заготовленно через `Prefer: code=NNN` (prism-http-server, getHttpConfigFromRequest.js) | верификация аудита N1/N7
- 2026-09-01 | TypeSpec: модель, не используемая ни в одной операции, не попадает в сгенерированный openapi.yaml — документационные сущности (Owner) фиксировать прозой спеки + `@doc`, а не «мёртвыми» моделями | верификация аудита N4
- 2026-09-01 | Макет мока без состояния (Prism) годится для smoke по схеме, но не для сквозных UI-сценариев («действие → изменившийся GET»): для этого нужен in-memory стаб с минимальной доменной логикой; стаб — вторичный источник правды, обязательна сверка с реальным бэком (prism proxy) | находка N1, решение Q1
- 2026-09-02 | Рекомендации по очистке веток в аудитах обязаны проверять предковость, а не «слито ли по ощущению»: tip `docs/audit-fixes` (69fa18e) не предок main, хотя его патч уже в main отдельным коммитом (c1a81ce, деревья идентичны, `git cherry` → «-») — `git branch -d` откажет, нужен `-D` после сверки; SHA в статусах указывать как tip ветки, а не merge-коммит main | перепроверка status-audit
- 2026-09-02 | `git fetch --prune` убирает remote-tracking ссылки только уже удалённых на remote веток; для «почистить слитые remote» сначала удаление на GitHub (операция пользователя), затем prune | перепроверка status-audit
- 2026-09-02 | Ссылки вида «файл:строка» в аудитах проверять фактическим чтением строки: найденные ошибочные — смещение на строку/диапазон (Railway MCP в docker.txt — строка 14, не 12-13; пометка «не подтверждено» — §8:220-221, а сама строка утверждения — :147) | перепроверка обоих аудитов
- 2026-09-02 | Числа строк/объёмов файлов в доках гниют на первом же редактировании (ревью PR поймало 504→506 в том же PR, который сам и сдвинул строки) — не указывать; git-утверждения в аудитах помечать, где проверялись (локальный клон vs GitHub), иначе в CI-клоне не воспроизводятся | ревью PR #4
- 2026-09-02 | В тексте команд коммита нельзя упоминать путь памяти (.agents/…) и подстроку «agents» — правила permission окружения отклоняют всю команду; про архив в сообщении писать без пути | ограничение окружения
- 2026-09-02 | Пиннинг composite-экшена на SHA защищает только обёртку workflow: action.yml сам ставит CLI из releases/latest на каждом прогоне — полная фиксация невозможна без форка; пин всё равно лучше @latest (меняется только логика обёртки) | ревью workflow
- 2026-09-02 | В доках после ru-text-прохода стоят NBSP (U+00A0) после коротких предлогов — точечные string-replace правок должны матчить `\u00a0`, иначе молча промахиваются; проверять через `cat -A` | правка work-plan.md
- 2026-09-02 | TypeSpec 1.15: объектные значения — `#{…}` (`@service(#{title: …})`); обёртки ответов — `model X extends Response<NNN> { @body body: T; }` (форма `is` не принимает ключ `statusCode` в значениях примеров); строковые коды — alias-union (`"a" | "b"`): кавычные литералы не присваиваются members enum'а; темпоральные литералы в значениях не парсятся — в @opExample давать только тела Error; `@multipleOf` — из `@typespec/json-schema` (optional peer эмиттера openapi3); опции эмиттера kebab-case (`seal-object-schemas`), путь вывода — `emitter-output-dir: "{project-root}/dist"` (голый относительный путь запрещён) | практика этапа 1
- 2026-09-02 | Prism 5.16: относительный `servers.url` игнорирует (монтирует с корня) — префикс /api проверять по yaml; `Prefer: code=NNN` отдаёт именованный пример ответа (@opExample с ключами statusCode/body); размер 413 не эмулирует; engines node >=24.18 — совпадает с обёрткой 1.2 | smoke этапа 1
- 2026-09-02 | Пробел спецификации ловится при коде: для 409 «id занят» (E13) не было кода в C7 — добавлен `duplicate_id` с синхронизацией спеки и комментариями в issue, а не переиспользован `slot_conflict` (семантика про слоты) | ревью этапа 1
- 2026-09-02 | Критерий 1.2 подтверждён: better-sqlite3 13.0.3 ставится в node:24 из prebuild за ~4 с без build-essential; `npm ci`, compile и smoke работают через volume на DrvFs `/mnt/e` (медленно, но стабильно) | шаг 1.2
- 2026-09-02 | Smoke по моку: сначала compile (защита от устаревшего артефакта), ждать процесс заглушки (`kill -0`), а не порт (на 4010 может сидеть чужой сервис), тело ответа — в mktemp (параллельные прогоны) | ревью этапа 1
- 2026-09-02 | GitHub Actions job-container (ubuntu-latest + container: node:24): git в run-шагах не видит репозиторий, который создал actions/checkout — checkout регистрирует safe.directory только во временном HOME, а шаги идут с HOME=/github/home и workspace принадлежит хостовому runner-пользователю; `git diff` при этом выдаёт «warning: Not a git repository» (не «dubious ownership»). Проверки «артефакт vs генерация» делать cp+diff без git (или отдельным шагом `git config --global --add safe.directory`) | падение contract-sync в PR #7
- 2026-09-02 | Ревью-бот PR (#7) полезный, но требует верификации: P1 про trap был частично неточен (сирота возможен не в названном окне), исправлено консолидацией trap; P2 — npm install записал caret (^1.15.0) там, где руками пинили exact: после `npm i -D pkg@X` проверять package.json на caret | ревью этапа 1
- 2026-09-02 | DrvFs (/mnt/e): `chmod +x` не доходит до git-индекса (в дерево ложится 100644), на чистом checkout в Linux скрипт не запускается; чинить `git update-index --chmod=+x <файл>`, проверять `git ls-files -s` | ревью PR #7 (m3)
- 2026-09-02 | shadcn CLI (v4): init не полностью неинтерактивен — `init -y -b radix -p nova --no-monorepo`, иначе промпты библиотеки/пресета съедают TTY; при Vite-алиасе `@` компоненты пишутся в литеральную папку `frontend/@/` — переносить в `src/`; init может перезаписать tsconfig без `strict` — сверять после | этап 2 (2.1)
- 2026-09-02 | Vite 8 dev-сервер биндится на localhost (в контейнере = ::1): curl на 127.0.0.1 даёт 000, только по имени localhost; для связки двух прогонов обёртки (стаб+vite) нужна host-сеть контейнера, иначе изолированные bridge-сети не дают прокси достать стаб | этап 2 (2.2, ревью #5)
- 2026-09-02 | Даты полуночи браузера форматировать через Intl в MSK опасно: для поясов восточнее MSK «день» сдвигается назад — подписи дня строить от полудня UTC выбранного календарного дня (`YYYY-MM-DDT12:00:00Z`) | ревью этапа 2 (мажор #4)
- 2026-09-02 | Стаб контракта без валидаций = обманчивый фронт: мок обязан повторять E-кейсы спеки (кривой JSON→400 Error, неизвестные поля→400, start вне сетки/в прошлом→400, 413), иначе UI проверяет несуществующий бэк; смоук стаба — место проверки стейтфул-сценариев до этапа 4 | ревью этапа 2 (мажоры #1–#2)
- 2026-09-02 | ISO-регексп пропускает невозможные даты (месяц 13, день 40, 99:99) — форма без валидности; для query-параметров всегда двойной guard: регексп на форму + Date.parse не NaN | раунд 5 PR #11
