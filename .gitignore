# Локальная настройка и внутренние файлы (в git не попадают)
.opencode/
.env
skills-lock.json

# Окружение
.venv/
venv/
__pycache__/
*.pyc
node_modules/

# SQLite data (PII — имена/email гостей)
backend/data/*.db

# Архив одноразовых аудитов (локально, в git не попадает)
.agents/archive/
